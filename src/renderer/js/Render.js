import fs from 'fs'
import path from 'path'
import puppeteer from 'puppeteer'

// import full html file for render to pdf
// setup pupeteer options
// render html file using puppeteer
// output .pdf file to .cache directory as temp.pdf
export default function Render(name, documentPath) {
  
}
